<td><%= link_to user.name, edit_user_path(user) %></td>
<td><%= link_to user.email, edit_user_path(user) %></td>
<td><%= link_to "X", user_path(user), method: :delete, class: 'red', data: { confirm: "Are you sure?" } %></td>
